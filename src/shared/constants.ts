export enum Constants {
  HOSTNAME = '127.0.0.1',
  PORT = 3000,
  ENCODING = 'utf8',
}

export enum Permissions {
  CREATE = 'create',
  READ = 'read',
  UPDATE = 'update',
  DELETE = 'delete',
}

export enum DataBase {
  DATABASE = 'some_db',
  DIALECT = 'mysql',
  USERNAME = 'root',
  PASSWORD = 'password',
  HOST = 'localhost',
}
